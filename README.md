# RedisCache.API
 Build a small .NET Web API that uses Redis as a distributed cache in front of a SQL database. Apply the cache-aside pattern and demonstrate how caching improves performance and how the system behaves when the cache fails.
